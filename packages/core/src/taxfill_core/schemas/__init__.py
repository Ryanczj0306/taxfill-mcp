"""Pydantic v2 schemas for TaxFill data files and workspace state.

- :mod:`taxfill_core.schemas.formpack` — the ``pack.yaml`` form-pack spec
  (dev plan section 5), shared by federal and state forms.
- :mod:`taxfill_core.schemas.profile` — the guided-intake profile
  (dev plan section 4), filled incrementally with per-answer provenance.
"""
