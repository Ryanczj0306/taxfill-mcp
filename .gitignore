# Python
__pycache__/
*.py[cod]
.venv/
dist/
build/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
# NOTE: uv.lock is committed on purpose (reproducible CI) — never ignore it.

# Shared blank-form download cache (fetch_blank). Official IRS/state PDFs
# are downloaded at runtime and NEVER committed to the repo.
.cache/

# macOS
.DS_Store

# User tax workspaces hold SSN-bearing documents and drafts (PII).
# They must NEVER be committable.
taxfill-workspace/

# staged data copy for packaging (source of truth is repo-root knowledge/ + formpacks/)
packages/core/src/taxfill_core/_data/
